/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://omaralibi.tn', // replace with your domain
  generateRobotsTxt: true, // generates robots.txt file
  sitemapSize: 7000, // optional: splits sitemaps if too large
  changefreq: 'daily',
  priority: 0.7,
};
